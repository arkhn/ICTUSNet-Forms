version: "3.8"

services:
  api:
    environment:
      - DJANGO_DEBUG=0

  app:
    build: ./app
    env_file:
      - ./app/.env
    volumes:
      - app_data:/app

  nginx:
    volumes:
      - app_data:/app
    depends_on:
      - app

volumes:
  app_data:
